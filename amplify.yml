version: 1
frontend:
  phases:
    build:
      commands:
        - mkdir -p dist
        - cp -R frontend/* dist/
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths: []
